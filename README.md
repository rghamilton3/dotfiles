# dotfiles
This branch is based on [Arch Linux](https://www.archlinux.org/)

```shell
git clone --recursive https://github.com/rghamilton3/dotfiles.git --branch arch --single-branch ~/.dotfiles
cd ~/.dotfiles
bash ./install.sh
```
