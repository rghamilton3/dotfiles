# dotfiles
This branch is based on CentOS

```shell
git clone --recursive https://github.com/rghamilton3/dotfiles.git --branch centos-6 --single-branch ~/.dotfiles
cd ~/.dotfiles
bash ./install.sh
```
